import React from 'react';
import { Wrapper } from './styled';

const Modal = ({click}) => {
    return (
        <Wrapper onClick={click}>
            hi
        </Wrapper>
    );
};

export default Modal;